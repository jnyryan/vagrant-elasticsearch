server {
  listen                *:8080 ;

  server_name           localhost;
  access_log            /var/log/nginx/test-site.log;

  location / {
    root  /var/www/test-site;
    index  index.html  index.htm;
  }
}